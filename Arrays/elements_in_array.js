    

     // this program finds the index of an element in an array using "indexOf"

     const colors = ["Red", "Green", "Blue"];
     const index = colors.indexOf("Green");
     if (index !== -1) {
         console.log("Green found at index: " + index);
     } else {
         console.log("Green not found");
     }
     