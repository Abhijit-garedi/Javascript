


     // this program sorts an array of numbers in ascending order usng the "sort" method

     const numbers = [5, 3, 8, 1, 2];
     numbers.sort((a, b) => a - b); // Sorts the array in ascending order
     console.log(numbers);
     