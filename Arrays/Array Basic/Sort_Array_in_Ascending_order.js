 
  // JS program which will sort an array ascending order

  let arr = [3, 1, 4, 1, 5, 9];
  let sortedArr = arr.sort((a, b) => a - b);
  console.log(sortedArr); 
  
  
    // Output: [1, 1, 3, 4, 5, 9]
  